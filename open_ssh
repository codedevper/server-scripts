sudo apt update
sudo apt upgrade -y

sudo apt install -y \
  curl \
  git \
  wget \
  unzip \
  zip \
  ca-certificates \
  openssh-server

echo "server ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/server-nopasswd
