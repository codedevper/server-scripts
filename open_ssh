apt update
apt upgrade -y

apt install -y \
  curl \
  git \
  wget \
  unzip \
  zip \
  ca-certificates \
  openssh-server

echo "$USER ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/sudo-nopasswd
